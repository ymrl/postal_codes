import { style } from "@vanilla-extract/css";

export const textWithIconStyle = style({
  display: "inline-flex",
  flexDirection: "row",
  justifyContent: "center",
  alignItems: "start",
  gap: "0.15em",
});
